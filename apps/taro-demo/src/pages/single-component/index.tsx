import { View } from '@tarojs/components';

export default function SingleComponentPage() {
  return (
    <View style={{ padding: 16 }}>
      这里预留给后续的单组件 demo（例如 Text/Image/ScrollView 单独验证）。
    </View>
  );
}
